<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gabia.mbaproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MBAProject">
        <activity
            android:name=".application.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".application.adminmodules.dashboard.AdminDashboardActivity"/>
        <activity android:name=".application.adminmodules.finance.FinanceHomeActivity"/>
        <activity android:name=".application.adminmodules.finance.MemberDetailActivity"/>
    </application>

</manifest>